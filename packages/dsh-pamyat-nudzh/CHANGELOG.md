## 0.1.0-alpha.12 — 04.09.2026

ПРИБОР РАНЬШЕ ПОРОГА (долг 93). Между тревогой и компактом на живом замере прошло НОЛЬ
ходов: тревога приходит внутри шага, вставка ждёт следующего, а его не случается — механизм
не помогает в том самом случае, ради которого заведён. Числа «сколько ходов обычно проходит»
не было ни у кого, поэтому чинить было нечем.

Теперь при снятии тревоги печатается, сколько шагов прошло с её подачи. Число копится в бою
само, и через неделю решение о лечении можно будет принимать по замеру, а не по догадке.

ГРАНИЦА, названная в коде: считаются шаги, ДОШЕДШИЕ до обработчика. Ход, оборванный раньше
(предел сессии, сигнал), в счёт не попадёт — значит это НИЖНЯЯ граница, а не точное число.

Счёт идёт, пока подана ТРЕВОГА, а не пока взведена вставка: вставка гасится на первом же
шаге, и счёт по ней замер бы на единице всегда — прибор показывал бы собственное устройство
вместо предмета. Проба A-трет ловит именно эту подмену; порча (счёт по взведённости) роняет
обе новые пробы разными сообщениями.

Стендов 5, проб 52 (было 50).

# Изменения

## 0.1.0-alpha.11

🔴 **`npm test` гонял ЧЕТЫРЕ стенда из пяти — `stend-vstavki.mjs` в перечень не попал.**
Перечень файлов был записан рукой, добавленный стенд в него не вошёл, и его молчание было
неотличимо от «стенд зелёный». Прогнала выпавший отдельно — 6 из 6, то есть красноты он не
скрывал; но это удача, а не устройство. Теперь прогон идёт ОБХОДОМ каталога `test/stend-*.mjs`,
как в ядре и у секретаря: расхождение перечня с диском стало невозможным по построению.
Третий экземпляр класса за сутки — долг 103.

## 0.1.0-alpha.10 — долг 94: имя и версия при монтаже

Пакет печатал `[dsh-pamyat-nudzh]` без номера. Правило приняли 03.09.2026 после слепого
выпуска в соседнем пакете и применили к четырём предметам семейства из шести — лечение
экземпляра вместо класса, и рецидив выглядел новой болезнью.

Цена была не косметической: 04.09 пришлось читать исходник на диске, чтобы установить,
какую редакцию нуджа держит боевой процесс, — по журналу это было нельзя.

Версия читается из своего манифеста, не константой: константа при следующем выпуске
утверждала бы номер, которому предмет уже не соответствует. Проба сверяет напечатанное
С МАНИФЕСТОМ; порча «версия константой» краснит её обоими числами.

Заодно заведён безусловный след подъёма. Прежде пакет говорил только при беде (предел
не задан) — то есть исправно смонтированный был неотличим от несмонтированного, и
«напоминание не пришло» читалось одинаково при обеих причинах.

Записи ведутся **от первой опубликованной версии**. Редакции, существовавшие до неё,
сюда не входят: они не уезжали к получателю, и их номера ничего не адресуют снаружи.

Источник дат — реестр npm (время публикации), источник содержания — работа, которой
эти выпуски вызваны. Даты в UTC.

## 0.1.0-alpha.9 — 2026-09-03

🔴 У меры появился ПОТРЕБИТЕЛЬ. До этой редакции пакет считал занятость и при переходе
порога звал `console.error` — строка уходила человеку в журнал и умирала там, а до
модели, которой напоминание адресовано, не доходило ничего. Механизм кричал исправно,
но в сторону, где никто не слушает; снаружи «работает» и «бесполезен» неотличимы.

Стало: хук `agent/pre-step`, вставка durable-сообщением через штатную фабрику
`createUserMessage`, РОВНО ОДИН раз за цикл тревоги, с числами занятости и предела.
Крик в stderr оставлен — это другой адресат, и по нему вставку потом доказывают.

Границы записаны в код и README: флаг гасится ДО сборки сообщения (иначе исключение
в сборке оставило бы напоминание на каждом шаге); при `predel: 0` вставки нет вовсе,
как и тревоги; счёт у пакета один на плагин, поэтому при двух сессиях разом
напоминание уйдёт в ту, чей шаг случится первым.

Добавлен `@deepseek-ai/dsh-llm` в `peerDependencies` — фабрика сообщений живёт там.
Новый стенд `test/stend-vstavki.mjs`: шесть проб, первая на заведомо исправном.

## 0.1.0-alpha.8 — 2026-09-03

Выпуск документационный, код не менялся: добавлен CHANGELOG.md (в состав), поле repository с directory. Номер поднят потому, что номер версии — имя предмета: правка манифеста или описания делает предмет другим, и сумма предмета при этом меняется всегда.

## 0.1.0-alpha.7 — 2026-09-03

🔴 Порог считается от ЗАНЯТОСТИ окна (вход последнего вызова), а не от суммы расхода по всем вызовам. Сумма растёт без предела и компакция её не уменьшает: в бою это дало «израсходовано НЕ МЕНЬШЕ 1255120 из 1000000» при двух учтённых вызовах и без всякого переполнения окна. Тревога теперь СНИМАЕТСЯ вслух, когда занятость вернулась под порог.

## 0.1.0-alpha.6 — 2026-09-03

Единица числа названа рядом с самим числом: сумма по вызовам — это счёт за работу, а не заполненность окна.

## 0.1.0-alpha.5 — 2026-09-03

Первый выпуск. Расходный учёт: не пора ли компактить. Отсутствие числа от провайдера считается отдельно и не превращается в ноль.
